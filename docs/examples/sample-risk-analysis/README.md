# sample-risk-analysis

This folder will hold a worked example for documentation purposes once the v0.2 reference runtime ships.

The example shows:
- Inputs the run consumed (under `inputs/`)
- Final output package (under `output/`)
- Run state snapshot (under `runtime-snapshot/`)
- Annotations explaining each artifact

For now, see the actual contracts and pipelines:
- See [docs/playbooks/onboarding-playbook.md](../../playbooks/onboarding-playbook.md)
